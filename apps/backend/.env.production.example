# Production Environment Variables (EXAMPLE - FILL WITH YOUR VALUES)
# Rename to .env.production when ready

# Your public Supabase URL
SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon key (public)
SUPABASE_ANON_KEY=your-anon-key

# Your Supabase service role key (KEEP SECRET)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Direct database connection URL if needed
DATABASE_URL=postgresql://postgres:password@your-project-id.supabase.co:5432/postgres

# Environment
NODE_ENV=production

# OpenAI API Key (KEEP SECRET)
OPENAI_API_KEY=your-openai-api-key

# Server Configuration
PORT=3000
ALLOWED_ORIGINS=https://your-website.com,https://www.your-website.com

# JWT Configuration (KEEP SECRET)
JWT_SECRET=your-jwt-secret-key-at-least-32-characters-long-and-random
JWT_EXPIRY=86400 # 24 hours in seconds 

# Sentry Configuration for Error Tracking
# Required in production for error monitoring
SENTRY_DSN=<your-sentry-dsn> 